#include <stdio.h>

/**
 * main - contains a program that prints all single digit numbers
 * Return: 0 (after execution)
 */

int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
	printf("%d", num);
	}
	printf("\n");
	return (0);
}
