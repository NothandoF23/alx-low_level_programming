#include "main.h"

/**
 * _abs - prints the absolute value of an interger
 * @num: is the variable passed/ argument
 * Return: the absolute value of num
 */

int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (num * (-1));
}
