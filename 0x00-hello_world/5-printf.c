#include <stdio.h>

/**
 * main - prints out text using the printf function
 * Return: 0 (on success)
 */

int main(void)
	{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}

